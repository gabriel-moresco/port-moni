<UserControl x:Class="PortMoni.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        DataContext="{Binding MainViewModel, Source={StaticResource Locator}}"
        d:DesignWidth="1280" d:DesignHeight="720">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVis"/>
    </UserControl.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadInfoCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" Grid.ColumnSpan="2">
            <MenuItem Header="Menu">
                <MenuItem Header="Menu1"/>
                <Separator/>
                <MenuItem Header="Fechar"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="Ajuda"/>
            </MenuItem>
        </Menu>
        <Grid Grid.Row="1" Grid.Column="0" Background="#424242">
            <StackPanel VerticalAlignment="Center">
                <Button Style="{DynamicResource MahApps.Metro.Styles.MetroCircleButtonStyle}" Command="{Binding OpenNewOperationViewCommand}" Height="50" Width="50" ToolTip="Adicionar Operação">
                    <iconPacks:PackIconIonicons Kind="AddiOS" Height="30" Width="30"/>
                </Button>
            </StackPanel>
        </Grid>
        <TabControl Grid.Row="1" Grid.Column="1">
            <TabItem Header="Produtos">
                <ScrollViewer>
                    <ItemsControl ItemsSource="{Binding ShareList}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Height="90">
                                    <Grid Height="1" Background="LightGray" VerticalAlignment="Top" Margin="0 -1 0 0"/>
                                    <Border CornerRadius="2" Background="LightBlue" Width="4" Margin="10 15 0 15" HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding}" FontSize="16" Foreground="LightBlue" FontWeight="Bold" HorizontalAlignment="Left" Margin="18 10 0 0"/>
                                    <TextBlock Text="SALDO ATUAL" FontSize="10" Foreground="LightGray" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="18 0 0 35"/>
                                    <TextBlock Text="R$ 1.254,42" FontSize="16" Foreground="LightBlue" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="18 0 0 13"/>
                                    <TextBlock Text="COTAÇÃO ATUAL" FontSize="10" Foreground="LightGray" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0 0 0 35"/>
                                    <TextBlock Text="{Binding CurrentQuote, StringFormat={}{0:C}, ConverterCulture='pt-BR'}" FontSize="16" Foreground="LightBlue"
                                           HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0 0 0 13"/>
                                    <TextBlock Text="RENTABILIDADE" FontSize="10" Foreground="LightGray" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 0 10 35"/>
                                    <TextBlock Text="51,63%" FontSize="16" Foreground="LightBlue" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 0 10 13"/>
                                    <Grid Height="1" Background="LightGray" VerticalAlignment="Bottom"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Carteira"/>
            <TabItem Header="Rentabilidade"/>
        </TabControl>
        <Grid Grid.Row="1" Grid.Column="1" Margin="0 40 0 0" Visibility="{Binding ProgressRingIsVisible, Converter={StaticResource boolToVis}, FallbackValue=Hidden}">
            <Grid Background="LightGray" Opacity="0.4"/>
            <Controls:ProgressRing Width="200"/>
        </Grid>
    </Grid>
</UserControl>